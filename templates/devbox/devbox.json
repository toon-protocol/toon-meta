{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.17.3/.schema/devbox.schema.json",
  "packages": ["nodejs@22", "pnpm@8.15.0"],
  "shell": {
    "scripts": {
      "build": "pnpm install --frozen-lockfile && pnpm build",
      "lint": "pnpm lint",
      "test": "pnpm test"
    }
  }
}
